{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: core defaults plus this shell's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
